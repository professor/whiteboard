<% content_for :title, 'Edit Page Comment Type' %>
<h1>Editing page comment type</h1>

<%= form_for(@page_comment_type) do |f| %>
    <% if @page_comment_type.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@page_comment_type.errors.count, "error") %> prohibited this comment type from being
            saved:</h2>

          <ul>
            <% @page_comment_type.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
      <%= f.label :name %><br/>
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.label :background_color %><br/>
      <%= f.text_field :background_color %>
    </p>
    <p>
      <%= f.submit "Update" %>
    </p>
<% end %>

<%= link_to 'Show', @page_comment_type %> |
<%= link_to 'Back', page_comment_types_path %>
