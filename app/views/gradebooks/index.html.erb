<table>
 <% @students.each do |student| %>
     <tr>
        <td><%=student.human_name%></td>
        <% GradeBook.get_scores(@course, student).each do |gradebook| %>
          <td><input value='<%= gradebook.score %>'/></td>
        <% end %>
    </tr>
  <%end%>
</table>
