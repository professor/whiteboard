<% content_for :title, 'New page' %>

<%= render :partial => "left_nav" %>

<h1>New page</h1>
<%= render :partial => "editable_form",:locals => { :button_name => "Create" } %> | <%= link_to 'Back', pages_path %>
