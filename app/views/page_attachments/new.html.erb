<%= content_for :title, "New page attachment" %>
<%= content_for :head do %>
<% end %>

<h1>New page attachment</h1>
<%= @page_attachment.page_id %>
<% unless current_user.is_student %>
    <br/>
   <strong>(This page only works for students. If you aren't on a team for a course, you can't upload a deliverable)</strong>
   <br/><br/>
<% end %>


<%= render :partial => "editable_form",:locals => { :button_name => "Create" } %>

<%= link_to 'Cancel', @page_attachment.page %>
<br/>
<br/>