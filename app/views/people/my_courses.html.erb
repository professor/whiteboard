<% content_for :title, 'My Courses for ' + @person.human_name %>

<h1>My courses</h1>
<% @registered_for_these_courses.each do |course| %>
    <p><%= link_to course.name, course_path(course) %></p>
    <%# render :partial => "/courses/show_actions" ,:locals => { :course => course } %>
<% end %>
<% @teaching_these_courses.each do |course| %>
    <p><%= link_to course.name, course_path(course) %></p>
    <%# render :partial => "/courses/show_actions" ,:locals => { :course => course } %>
<% end %>


<br/>