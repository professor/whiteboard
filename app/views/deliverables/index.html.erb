<h1>Listing Deliverables</h1>

<% if current_user.is_staff %>
    <div class="staff"><p>Note that this view is intended for students. To find deliverables submitted for a course,
      first select the course and then pick "deliverabes.
      See" <%= link_to "My courses", my_courses_path(current_user) %></p></div>
<% end %>

<%= link_to 'New deliverable', new_deliverable_path %>

<% if !@current_deliverables.empty? %>
    <h2><%= @current_semester %> <%= @current_year %>Current Semester Deliverables</h2>
    <%= render :partial => "deliverable_listing_student", :locals => {:assignments => @current_assignments, :skip_course_column => false, :courses => @current_courses, :user => current_user} %>
    <br>
<% end %>
<% if !@past_deliverables.empty? %>
    <h2>Previous Semesters' Deliverables</h2>
    <%= render :partial => "deliverable_listing_student", :locals => {:assignments => @past_assignments, :skip_course_column => false, :courses => @past_courses, :user => current_user} %>
    <br><br>
<% end %>

<br/>

<%= link_to 'New deliverable', new_deliverable_path %>