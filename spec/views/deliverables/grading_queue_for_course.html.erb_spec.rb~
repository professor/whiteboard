require 'spec_helper'

describe "deliverables/grading_queue_for_course.html.erb" do
  before(:each) do

  end

  describe "The page should have a filter" do
    expect(page).to have_content 'filter'
  end

end
